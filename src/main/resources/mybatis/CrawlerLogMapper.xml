<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pab.framework.crawlerdb.dao.CrawlerLogDao">
    <insert id="save" parameterType="com.pab.framework.crawlerdb.domain.CrawlerLog">
        insert into crawler_log(flow_id,action_id,oper_time,result_code,result_message)
        values (#{flowId},#{actionId},#{operTime},#{resultCode},#{resultMessage});
    </insert>
    <delete id="deleteByCrawlerDateTime">
        delete from crawler_log where oper_time &lt; #{crawlerDateTime}
    </delete>
</mapper>
